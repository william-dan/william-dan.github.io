# William Dan

Personal website and future blog archive for William Dan, built with Next.js and Three.js.

## Development

```bash
pnpm install
pnpm dev
```

## Static Export

This repository is configured for GitHub Pages deployment through a GitHub Actions workflow.

```bash
pnpm build
pnpm preview
```

The static export is written to `out/`.
